# questionnaire verifier rubric

- require: answers
- require: control_tags
- Every answer with `needs_human=false` must include at least one brain citation path.
- Unsupported questions must set `needs_human=true` and must not include fabricated citations.
