# Standards baseline

Organizational and industry standards that inform control selection and policy language (e.g., NIST 800-53 baselines, CIS benchmarks, internal secure-configuration standards). Add Markdown or YAML references here; crosswalk to frameworks in `01-frameworks/` and controls in `02-controls/`.
