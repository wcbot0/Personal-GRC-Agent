# Personal GRC Agent v0.1.1

**Release date:** 2026-06-13

Public-release hygiene release — scrubbed published git history and clarified privacy expectations for forks.

## Security

- Rewrote published git history to remove accidental local paths, committed runtime artifacts, and personal identifiers from reachable refs
- Untracked `.cursor/` — generate local Cursor glue with `spa init --runtime cursor`

## Documentation

- README privacy and forking guidance
- `CODEOWNERS` fork instructions

## Upgrade

Use **`v0.1.1`** (or `main`) — do not use the pre-scrub `v0.1.0` tag. If you cloned before this release, re-clone or:

```bash
git fetch origin
git reset --hard origin/main
```

See [CHANGELOG.md](https://github.com/wcbot0/Personal-GRC-Agent/blob/main/CHANGELOG.md) for full history.
